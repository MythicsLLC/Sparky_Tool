# Sparky Tool — Claude Code Guidelines

## Commit behaviour

After completing any code change, **always create a git commit automatically** without asking for confirmation. Stage only the files that were modified or created for that task. Do not push — the user pushes manually.

Use conventional commit messages (`feat:`, `fix:`, `refactor:`, etc.) with a short body summarising the why.
